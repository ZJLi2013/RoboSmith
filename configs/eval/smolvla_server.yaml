# SmolVLA model server config for vla-eval-harness
#
# Usage:
#   vla-eval serve --config configs/eval/smolvla_server.yaml

model_server: "scripts.part3.smolvla_server:create_model_server"

args:
  checkpoint: "/path/to/smolvla/checkpoint"
  dataset_id: "user/franka-pick-100ep-genesis"
  action_horizon: 1
  device: "cuda"
